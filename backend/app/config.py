# backend/app/config.py
# Application settings loaded from environment variables via Pydantic Settings
# Centralizes all configuration (API keys, DB URLs, defaults)
# Related: main.py, convex_client.py

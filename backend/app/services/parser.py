# backend/app/services/parser.py
# Parses procurement documents (PDF, DOCX, XLSX) into structured text using Docling
# Core service in the extraction pipeline — converts raw files to LLM-ready markdown
# Related: extraction.py, pipeline.py
